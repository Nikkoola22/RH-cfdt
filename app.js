const App = () => <h1>Bonjour, le fichier HTML est bien ouvert avec React sur Mac !</h1>;
const container = document.getElementById('root');
const root = ReactDOM.createRoot(container);
root.render(<App />);